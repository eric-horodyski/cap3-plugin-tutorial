import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cap3-plugin-tutorial',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
